<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- Basic dark theme colors -->
  <Color x:Key="App.BackgroundColor">#FF1E1E1E</Color>
  <Color x:Key="App.ForegroundColor">#FFF5F5F5</Color>
  <Color x:Key="App.AccentColor">#FF58A6FF</Color>

  <SolidColorBrush x:Key="App.Background" Color="{StaticResource App.BackgroundColor}" />
  <SolidColorBrush x:Key="App.Foreground" Color="{StaticResource App.ForegroundColor}" />
  <SolidColorBrush x:Key="App.Accent" Color="{StaticResource App.AccentColor}" />

  <Style TargetType="Window">
    <Setter Property="Background" Value="{StaticResource App.Background}" />
    <Setter Property="Foreground" Value="{StaticResource App.Foreground}" />
  </Style>
  <Style TargetType="TextBlock">
    <Setter Property="Foreground" Value="{StaticResource App.Foreground}" />
  </Style>
  <Style TargetType="Button">
    <Setter Property="Foreground" Value="{StaticResource App.Foreground}" />
    <Setter Property="Padding" Value="8,4" />
    <Setter Property="Margin" Value="4" />
  </Style>
</ResourceDictionary>